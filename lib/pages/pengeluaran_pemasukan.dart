import 'package:flutter/material.dart';

// ignore: camel_case_types
class Pengeluaran_Pemasukan extends StatelessWidget {
  const Pengeluaran_Pemasukan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Input Pengeluaran dan Pemasukkan'),
      ),
    );
  }
}
