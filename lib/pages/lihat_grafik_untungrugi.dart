import 'package:flutter/material.dart';

class LihatGrafikUntungRugi extends StatelessWidget {
  const LihatGrafikUntungRugi ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lihat Grafik Untung Rugi'),
      ),
    );
  }
}
